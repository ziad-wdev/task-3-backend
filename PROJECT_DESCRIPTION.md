# Express.js Project

This is a Node.js project built with the Express.js framework, utilizing TypeScript and Bun as the package manager. It uses Handlebars (HBS) for templating and includes functionality for making HTTP requests (axios) and geolocation (geoip-lite). Development is facilitated with `nodemon` for live reloading.
